<%= form_for(@abdominal_condition) do |f| %>
  <% if @abdominal_condition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@abdominal_condition.errors.count, "error") %> prohibited this abdominal_condition from being saved:</h2>

      <ul>
      <% @abdominal_condition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
