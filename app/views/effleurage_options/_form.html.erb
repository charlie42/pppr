<%= form_for(@effleurage_option) do |f| %>
  <% if @effleurage_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@effleurage_option.errors.count, "error") %> prohibited this effleurage_option from being saved:</h2>

      <ul>
      <% @effleurage_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
