<%= form_for(@constitution_option) do |f| %>
  <% if @constitution_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@constitution_option.errors.count, "error") %> prohibited this constitution_option from being saved:</h2>

      <ul>
      <% @constitution_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
