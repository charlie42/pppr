<%= form_for(@subcutanious_fat_option) do |f| %>
  <% if @subcutanious_fat_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subcutanious_fat_option.errors.count, "error") %> prohibited this subcutanious_fat_option from being saved:</h2>

      <ul>
      <% @subcutanious_fat_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
